{namespace todo.views}

/**
 * @param value
 * @param onClick
 */
{template .square}
  <button class="square" onclick={$onClick}>
    {$value}
  </button>
{/template}

/**
 * @param squares
 */
{template .board}
  <div>
    <div className="board-row">
      {$squares[0].render}
      {$squares[1].render}
      {$squares[2].render}
    </div>
    <div className="board-row">
      {$squares[3].render}
      {$squares[4].render}
      {$squares[5].render}
    </div>
    <div className="board-row">
      {$squares[6].render}
      {$squares[7].render}
      {$squares[8].render}
    </div>
  </div>
{/template}

/**
 * @param board
 * @param moves
 * @param status
 */
{template .game}
  <div class="game">
    <div class="game-board">
      {$board.render}
    </div>
    <div class="game-info">
      <div>{$status}</div>
      <ol>
        {foreach $move in $moves}
          <li key="{index($move)}">
            <button onclick={$move.onClick}>{$move.desc}</button>
          </li>
        {/foreach}
      </ol>
    </div>
  </div>
{/template}
